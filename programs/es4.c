#include <stdio.h>
#include <stdlib.h>

struct node {
	int data;
	struct node *next;
};

typedef struct node Node;

 lista_crea(

int main(int argc, char *argv[]) {
	int n;
	printf("Inserisci numero da 0 a 10: ");
	scanf("%d", &n);
	
	
	return 0;
}